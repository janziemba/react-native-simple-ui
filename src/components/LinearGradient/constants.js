// @flow

import type { ObjectOfStringsType } from '../../types';

/* eslint-disable import/prefer-default-export */

export const DIRECTIONS: ObjectOfStringsType = {
    horizontal: 'horizontal',
    vertical: 'vertical',
};
